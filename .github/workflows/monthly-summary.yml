name: Monthly Summary

on:
  schedule:
    # 1 לכל חודש ב-09:00 שעון ישראל (קיץ = UTC+3) → 06:00 UTC
    - cron: '0 6 1 * *'
  workflow_dispatch:

jobs:
  summary:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Set run date
        run: echo "RUN_DATE=$(date +%Y-%m-%d)" >> $GITHUB_ENV

      - name: Check if already sent today
        id: cache
        uses: actions/cache@v4
        with:
          path: .sent-marker
          key: monthly-${{ env.RUN_DATE }}

      - name: Checkout
        if: steps.cache.outputs.cache-hit != 'true'
        uses: actions/checkout@v4

      - name: Set up Python
        if: steps.cache.outputs.cache-hit != 'true'
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Run Monthly Summary
        if: steps.cache.outputs.cache-hit != 'true'
        env:
          MONDAY_API_TOKEN:             ${{ secrets.MONDAY_API_TOKEN }}
          MONDAY_BOARD_ID:              ${{ secrets.MONDAY_BOARD_ID }}
          MONDAY_TRANSACTIONS_BOARD_ID: ${{ secrets.MONDAY_TRANSACTIONS_BOARD_ID }}
          MONDAY_SOURCE_COL:            ${{ secrets.MONDAY_SOURCE_COL }}
          MONDAY_DATE_COL:              ${{ secrets.MONDAY_DATE_COL }}
          META_ACCESS_TOKEN:            ${{ secrets.META_ACCESS_TOKEN }}
          META_AD_ACCOUNT_ID:           ${{ secrets.META_AD_ACCOUNT_ID }}
          TG_TOKEN:                     ${{ secrets.TG_TOKEN }}
          TG_CHAT_ID:                   ${{ secrets.TG_CHAT_ID }}
        run: python scripts/monthly_summary_cloud.py

      - name: Mark as sent
        if: steps.cache.outputs.cache-hit != 'true'
        run: echo "sent" > .sent-marker
